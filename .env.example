# Copy to .env for local Docker Compose use. Use a generated secret; never
# commit the populated .env file.
POSTGRES_PASSWORD=replace-with-a-long-random-local-secret
POSTGRES_PORT=5432
API_PORT=8080
WEB_PORT=3000
CORS_ORIGIN=http://localhost:3000
